@page
@model SistemInformaticPensiune.Pages.TipuriCamera.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>TipCamera</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="TipCamera.ID" />
            <div class="form-group">
<span class="text-danger">*</span>
                <label asp-for="TipCamera.Tip" class="control-label"></label>
                <input asp-for="TipCamera.Tip" class="form-control" aria-required="true"/>
                <span asp-validation-for="TipCamera.Tip" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
