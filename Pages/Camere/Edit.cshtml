@page
@model SistemInformaticPensiune.Pages.Camere.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Camera</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Camera.ID" />
            <div class="form-group">
<span class="text-danger">*</span>
                <label asp-for="Camera.Numar" class="control-label"></label>
                <input asp-for="Camera.Numar" class="form-control" aria-required="true"/>
                <span asp-validation-for="Camera.Numar" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Camera.Price" class="control-label"></label>
                <input asp-for="Camera.Price" class="form-control" />
                <span asp-validation-for="Camera.Price" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Camera.TipCameraID" class="control-label"></label>
                <select asp-for="Camera.TipCameraID" class="form-control" asp-items="ViewBag.TipCameraID" ></select>
                <span asp-validation-for="Camera.TipCameraID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
